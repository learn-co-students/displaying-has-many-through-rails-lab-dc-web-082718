<%= @patient.name %>

<ul>
<% @patient.appointments.each do |a| %>
  <li><%= link_to a.doctor.name, doctor_path(a.doctor.id) %> <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></li>
<% end %>
</ul>
