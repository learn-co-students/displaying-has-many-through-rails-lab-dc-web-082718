
<h1> Doctor </h1><br />
 Name: <%= @doctor.name %><br/>
 Department: <%= @doctor.department %> <br /><br/>

 Appointments:

 <ul>
 <% @doctor.appointments.each do |appt| %>
     <li><%= link_to appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") + ", Patient: " + appt.patient.name, patient_path(appt.patient) %></li>
 <% end %>
 </ul>
 <br />
