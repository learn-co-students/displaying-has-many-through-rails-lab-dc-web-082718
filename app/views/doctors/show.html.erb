<%= @doctor.name %>

<ul>
<% @doctor.appointments.each do |a| %>
  <li> <%= link_to a.patient.name, patient_path(a.patient.id) %>
    <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </li>
<% end %>
  </ul>
